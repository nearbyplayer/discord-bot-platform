{
  "capabilities": {
    "db": { "requires": [] },
    "settings": { "requires": ["db"] },
    "permissions": { "requires": [] }
  },
  "features": {
    "department": {
      "requires": ["permissions", "settings"],
      "subfeatures": {
        "loa": { "requires": [] },
        "logs": { "requires": [] },
        "pager": { "requires": [] },
        "action": { "requires": [] }
      }
    },
    "ingame": { "requires": [] },
    "autolog": { "requires": ["ingame", "department/logs"] },
    "watchlist": { "requires": ["ingame", "department"] }
  },
  "sets": {
    "ingame-suite": ["autolog", "watchlist"]
  }
}
